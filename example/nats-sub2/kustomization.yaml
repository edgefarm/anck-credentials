# yaml-language-server: https://json.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: example-nats-sub-2

resources:
  - ../../manifest/nats-sub
